{
  "schema": "eip-8282-assurance-contract-v1",
  "authority": "Lean theorem statements and proofs are authoritative; metadata classifies but never closes evidence.",
  "objective": "Prove an abstract Lean model of the two EIP-8282 predeploys for the three public guarantees, then a Verity correspondence, or classify the gap. All three IDs additionally carry an EVM layer: concrete traces of the pinned runtime bytecode under EvmYul.EVM.Xi, refutable by mutating those bytes. Verity is OPEN for all three; deployment provenance is OPEN.",
  "guarantees": [
    {
      "id": "P-SUBMIT-1",
      "parent": "Eip8282.Audit.Guarantees.PSubmit1.psubmit1_bytecode_parent",
      "parent_note": "The registered parent is the bytecode theorem. The abstract-model theorem below is supporting evidence, not a substitute: per audit/WAVE0.md, abstract CHECKED does not stand in for bytecode CHECKED.",
      "assumptions": [
        "A-ABSTRACT-TX",
        "A-REACHABLE",
        "A-CLASSICAL-CHOICE",
        "A-NATIVE-DECIDE",
        "A-EVM-WORLD",
        "A-PINNED-SOURCE"
      ],
      "reproduction": {
        "command": "lake build EvmYul.FFI.ffi:dynlib && lake build Eip8282.Audit.Guarantees.PSubmit1 Eip8282.Tests.PSubmit1Mutant Eip8282.Audit.Trust",
        "expected": "psubmit1_bytecode_parent and the bytecode kill-line build; Trust prints propext, Classical.choice, Quot.sound plus one <theorem>._native.native_decide.ax_1_1 for each bytecode theorem (parent, Wave-1 getter mutant, Wave-4 LOG0 mutant, and independence)",
        "prerequisite": "The first target builds EVMYulLean's keccak/sha2 FFI shared objects. native_decide runs the compiled interpreter, which needs them; lakefile.lean passes both via --load-dynlib, libleanffi.so first."
      },
      "summary": "While enabled, a non-system write succeeds exactly on well-formed paid input; a success retains msg.value, appends exactly one authentic record (deposit calldata verbatim, or msg.sender||pubkey for an exit), and emits one anonymous LOG0 of that record (184-byte deposit calldata / 68-byte exit source||pubkey); every revert leaves state unchanged. Inhibited non-system calls revert.",
      "abstract": {
        "status": "CHECKED",
        "theorem": "Eip8282.Audit.Guarantees.PSubmit1.success_count_and_balance"
      },
      "evm": {
        "status": "CHECKED",
        "scope": "CONCRETE_TRACES",
        "theorem": "Eip8282.Audit.Guarantees.PSubmit1.psubmit1_bytecode_parent",
        "executes": "EvmYul.EVM.Xi (EVMYulLean f7e4ee0dc8f8d5265ce822a937ab5be771f182e9) over the pinned runtime bytes of pinned/bytecode/builder_deposits/main.hex and builder_exits/main.hex, driven by Eip8282.Audit.EvmRunner.",
        "world": "Two accounts (predeploy plus caller 0x1234); storage image excess=100 count=5 head=7 tail=9; gas 30000000; fuel 80000. The inhibited variant sets SLOT_EXCESS to 2^256-1.",
        "kill_line": {
          "module": "Eip8282.Tests.PSubmit1Mutant",
          "theorem": "Eip8282.Tests.PSubmit1Mutant.mutant_refutes_parent",
          "mutation": "two single-byte cuts, each fed to the same submitFacts the parent is registered against: builder_deposits runtime byte 158, RETURN (0xf3) -> REVERT (0xfd), the instruction that ends the fee-getter path; and builder_deposits runtime byte 274, PUSH1 operand 184 -> 0, the RECORD_SIZE that sizes the user-path LOG0. The calldatacopy size at offset 269, the LOG0 opcode at offset 276, the exit user-path RECORD_SIZE at offset 215, and P-DRAIN-1's system RETURN size at exit offset 450 are left alone.",
          "effect": "submitFacts mutatedDepositRuntime exitRuntime = false, and the fee getter reverts instead of succeeding. submitFacts logSizeMutatedDeposit exitRuntime = false: the paid deposit still appends the six calldata words, but LOG0 data size is 0 instead of 184, so depositPaidLogFact fails. The mutations are to bytecode bytes, not to any model function, and each mutant is fed to the same submitFacts the parent is registered against.",
          "independence": "Eip8282.Tests.PSubmit1Mutant.log_mutant_leaves_siblings_intact proves the LOG0-size mutant leaves Eip8282.Audit.Guarantees.PDrain1.drainFacts and Eip8282.Audit.Guarantees.PControl1.controlFacts true. P-DRAIN-1 sizes the system RETURN at exit offset 450; P-CONTROL-1 does not observe receipts. The new conjunct is not a restatement of a sibling guarantee."
        }
      },
      "verity": {
        "status": "OPEN",
        "theorem": null
      },
      "fidelity": {
        "covered": [
          "abstract: revert leaves persistent state unchanged; a successful write appends one record and retains full value; exit sourceAddress equals caller; inhibited user calls revert",
          "bytecode: empty calldata with value=0 returns exactly 32 bytes and leaves slots 0-3 unchanged, at the fake_exponential values 357 (deposit) and 427 (exit)",
          "bytecode: empty calldata with value!=0 reverts, so no storage write survives",
          "bytecode: a paid 184-byte deposit appends the six CALLDATALOAD words verbatim at QUEUE_OFFSET + tail*6 and bumps only SLOT_COUNT and SLOT_TAIL",
          "bytecode: a paid 48-byte exit writes msg.sender into the first appended slot, then the two pubkey words, at QUEUE_OFFSET + tail*3",
          "bytecode: with SLOT_EXCESS = INHIBITOR, both the getter and the paid write path revert on both predeploys",
          "bytecode: a successful paid deposit emits one anonymous LOG0 (0 topics) whose data is the 184-byte calldata; a successful paid exit emits one anonymous LOG0 whose 68-byte data is msg.sender || pubkey"
        ],
        "missing": [
          "the bytecode facts are concrete traces at one storage image; they are not universally quantified over storage, caller, value or calldata",
          "the system-call drain path is not exercised here (see P-DRAIN-1)",
          "no equivalence proof between Eip8282.Audit.Model.userCall and the bytecode layer; the two layers are independent evidence",
          "constructor/init bytecode and deployed-address provenance",
          "Verity Contract.run"
        ]
      },
      "classification": {
        "kind": "PARTIAL",
        "work": "The bytecode layer is real but finite. Do not read psubmit1_bytecode_parent as a universally quantified P-SUBMIT-1; it is a mutation-discriminating trace suite over the pinned runtimes."
      },
      "next_gate": "Generalise the bytecode facts from one storage image to symbolic or inductive coverage.",
      "roadmap_priority": "NOW"
    },
    {
      "id": "P-DRAIN-1",
      "parent": "Eip8282.Audit.Guarantees.PDrain1.pdrain1_bytecode_parent",
      "parent_note": "The registered parent is the bytecode theorem. The abstract-model theorem below is supporting evidence, not a substitute: per audit/WAVE0.md, abstract CHECKED does not stand in for bytecode CHECKED.",
      "assumptions": [
        "A-ABSTRACT-TX",
        "A-REACHABLE",
        "A-CLASSICAL-CHOICE",
        "A-NATIVE-DECIDE",
        "A-EVM-WORLD",
        "A-PINNED-SOURCE"
      ],
      "reproduction": {
        "command": "lake build EvmYul.FFI.ffi:dynlib && lake build Eip8282.Audit.Guarantees.PDrain1 Eip8282.Tests.PDrain1Mutant Eip8282.Audit.Trust",
        "expected": "pdrain1_bytecode_parent and the bytecode kill-line build; Trust prints propext, Classical.choice, Quot.sound plus one <theorem>._native.native_decide.ax_1_1 for each bytecode theorem",
        "prerequisite": "The first target builds EVMYulLean's keccak/sha2 FFI shared objects. native_decide runs the compiled interpreter, which needs them; lakefile.lean passes both via --load-dynlib, libleanffi.so first."
      },
      "summary": "Only the system-call path consumes queued records. On the pinned runtimes, a SYSTEM_ADDR call returns and removes the oldest min(queue_length, cap) records as a contiguous RECORD_SIZE buffer (68-byte exits / 184-byte deposits), advances QUEUE_HEAD by that many (or zeroes both pointers on a full drain), leaves QUEUE_TAIL and the unreturned suffix in place, and recodes only the deposit amount field from big-endian storage to little-endian return bytes.",
      "abstract": {
        "status": "CHECKED",
        "theorem": "Eip8282.Audit.Guarantees.PDrain1.fifo_bounded"
      },
      "evm": {
        "status": "CHECKED",
        "scope": "CONCRETE_TRACES",
        "theorem": "Eip8282.Audit.Guarantees.PDrain1.pdrain1_bytecode_parent",
        "executes": "EvmYul.EVM.Xi (EVMYulLean f7e4ee0dc8f8d5265ce822a937ab5be771f182e9) over the pinned runtime bytes of pinned/bytecode/builder_deposits/main.hex and builder_exits/main.hex, driven by Eip8282.Audit.EvmRunner. System drains go through EvmRunner.runDepositSystem / runExitSystem, which differ from the user runners only in msg.sender (SYSTEM_ADDR 0xfffffffffffffffffffffffffffffffffffffffe) and zero value.",
        "world": "Two accounts (predeploy plus caller); SYSTEM_ADDR for drains and caller 0x1234 for the user-does-not-drain check; gas 30000000; fuel 80000 for the under-cap and empty-queue traces, 300000 for the 65-deposit over-cap drain. Storage images hold SLOT_EXCESS=100, SLOT_COUNT=5 and a FIFO of distinctive records at QUEUE_HEAD=0 / QUEUE_TAIL in {0,1,2,17} on the exit side and {0,1,2,65} on the deposit side. Exit records occupy three slots from QUEUE_OFFSET=4; deposit records occupy six. Excess still folds to the sibling-known post-state (97 deposits / 103 exits) so the drain claim is isolated to the return buffer and the two queue pointers.",
        "kill_line": {
          "module": "Eip8282.Tests.PDrain1Mutant",
          "theorem": "Eip8282.Tests.PDrain1Mutant.mutant_refutes_parent",
          "mutation": "four single-byte cuts, each fed to the same drainFacts the parent is registered against: builder_exits runtime byte 244, PUSH1 operand 16 -> 8, the MAX_PER_BLOCK clamp that only the over-cap system drain loads; builder_exits runtime byte 450, PUSH1 operand 68 -> 64, the RECORD_SIZE multiplier of the system RETURN; builder_deposits runtime byte 304, PUSH1 operand 64 -> 32, the MAX_PER_BLOCK clamp that only the over-cap deposit drain loads; and builder_deposits runtime byte 483, PUSH1 operand 2 -> 9, the QUEUE_HEAD slot of the partial-drain SSTORE, retargeted onto QUEUE_OFFSET+5 (last remaining word of drained deposit item 0). The exit comparison immediate at offset 237, the deposit comparison immediate at offset 296, the user-path LOG0 RECORD_SIZE at offset 215, and the empty-queue reset PUSH1 QUEUE_HEAD operand at offset 494 are left alone.",
          "effect": "drainFacts depositRuntime capMutatedExit = false, drainFacts depositRuntime recSizeMutatedExit = false, drainFacts capMutatedDeposit exitRuntime = false, and drainFacts headSlotMutatedDeposit exitRuntime = false. With the exit-cap cut, seventeen queued exits return 8*68=544 bytes and QUEUE_HEAD advances to 8 instead of 16, while the under-cap two-record drain is untouched (cap_mutant_halves_the_over_cap_drain). With the record-size cut, the drain loop still walks the same records but the system RETURN reports n*64 bytes, so the two-record drain is 128 instead of 136 and the over-cap drain is 1024 instead of 1088 (rec_size_mutant_shrinks_the_return). With the deposit-cap cut, sixty-five queued deposits return 32*184=5888 bytes and QUEUE_HEAD advances to 32 instead of 64, while the empty-queue and under-cap deposit drains are untouched (deposit_cap_mutant_halves_the_over_cap_drain). With the head-slot cut, sixty-five queued deposits still return 11776 bytes but the advanced head value 64 is written into slot 9 instead of QUEUE_HEAD, so slot 2 stays 0, slot 9 becomes 64, and staleDepositRestIs 0 fails, while the empty-queue and under-cap full drains take the reset path and stay true (head_slot_mutant_overwrites_a_drained_word). The mutations are to bytecode bytes, not to any model function.",
          "independence": "Eip8282.Tests.PDrain1Mutant.drain_mutants_leave_siblings_intact proves all four mutants leave Eip8282.Audit.Guarantees.PSubmit1.submitFacts and Eip8282.Audit.Guarantees.PControl1.controlFacts true. P-SUBMIT-1 never calls from SYSTEM_ADDR. P-CONTROL-1 does, but holds QUEUE_HEAD=QUEUE_TAIL=0, so the cap clamp is never taken, the partial-drain head store is never taken, and 0*RECORD_SIZE is still 0: the P-DRAIN-1 parent is not a restatement of a sibling guarantee."
        }
      },
      "verity": {
        "status": "OPEN",
        "theorem": null
      },
      "fidelity": {
        "covered": [
          "abstract: system call never reverts; FIFO take/drop with per-kind cap; full drain empties the logical queue; deposit amount LE conversion; exit records unchanged",
          "bytecode: empty exit queue, SYSTEM_ADDR, empty calldata: success, 0-byte return, SLOT_EXCESS 100 -> 103, SLOT_COUNT 5 -> 0, both queue pointers stay 0",
          "bytecode: two queued exits return 136 bytes in FIFO order (record 0 then record 1, 68 bytes each: 20-byte source then pubkey words) and a full drain zeroes QUEUE_HEAD and QUEUE_TAIL",
          "bytecode: seventeen queued exits return exactly 16 records (1088 bytes), QUEUE_HEAD advances to 16, QUEUE_TAIL stays 17, first returned record is item 0 and last is item 15, so the window is the oldest sixteen; the first two words of drained item 0 and the first word of drained item 15 remain in storage",
          "bytecode: a user fee-getter on a two-record exit queue returns 32 bytes and leaves head=0 tail=2, so only SYSTEM_ADDR consumes the queue",
          "bytecode: one queued deposit returns 184 bytes with the amount field recoded from big-endian 0x0102030405060708 to little-endian 08 07 06 05 04 03 02 01, and a full drain zeroes both pointers",
          "bytecode: two queued deposits return 368 bytes with amounts in FIFO order",
          "bytecode: empty deposit queue, SYSTEM_ADDR, empty calldata: success, 0-byte return, SLOT_EXCESS 100 -> 97, SLOT_COUNT 5 -> 0, both queue pointers stay 0",
          "bytecode: sixty-five queued deposits return exactly 64 records (11776 bytes), QUEUE_HEAD advances to 64, QUEUE_TAIL stays 65, first returned amount is item 0 and last is item 63, so the window is the oldest sixty-four; all six words of drained item 0, the first word of drained item 63, and the first word of still-queued item 64 remain in storage"
        ],
        "missing": [
          "the bytecode facts are concrete traces at a finite family of storage images; they are not universally quantified over storage, caller, value or calldata",
          "30_000_000 gas bound as an EVM resource fact",
          "stale-slot non-erasure is pinned for deposit item 0 (all six words), deposit item 63 first word, still-queued deposit item 64 first word, and exit items 0 (first two words) and 15 (first word) after the over-cap drains; remaining drained words of those items and of other drained indices are not asserted",
          "EIP-7685 request-type prefixing",
          "no equivalence proof between Eip8282.Audit.Model.systemCall and the bytecode layer; the two layers are independent evidence",
          "Verity Contract.run"
        ]
      },
      "classification": {
        "kind": "PARTIAL",
        "work": "The bytecode layer is real but finite. Do not read pdrain1_bytecode_parent as a universally quantified P-DRAIN-1; it is a mutation-discriminating trace suite over the pinned runtimes at a handful of queue depths. Both per-kind caps (exit 16, deposit 64) and the partial-drain QUEUE_HEAD store are now on the kill-line."
      },
      "next_gate": "Generalise from the fixed queue depths to symbolic or inductive coverage, and assert stale-slot non-erasure for every remaining word of every drained record.",
      "roadmap_priority": "NOW"
    },
    {
      "id": "P-CONTROL-1",
      "parent": "Eip8282.Audit.Guarantees.PControl1.pcontrol1_nonempty_bytecode_parent",
      "parent_note": "Wave-5 nonempty-queue parent. The Wave-1 empty-queue parent Eip8282.Audit.Guarantees.PControl1.pcontrol1_bytecode_parent remains checked and is not weakened; the registered parent is now the strictly stronger nonempty-queue theorem which implies the empty-queue excess fold but also requires a nonzero drain. Per audit/WAVE0.md, abstract CHECKED does not stand in for bytecode CHECKED.",
      "assumptions": [
        "A-ABSTRACT-TX",
        "A-REACHABLE",
        "A-CLASSICAL-CHOICE",
        "A-NATIVE-DECIDE",
        "A-EVM-WORLD",
        "A-PINNED-SOURCE"
      ],
      "reproduction": {
        "command": "lake build EvmYul.FFI.ffi:dynlib && lake build Eip8282.Audit.Guarantees.PControl1 Eip8282.Tests.PControl1Mutant Eip8282.Audit.Trust",
        "expected": "pcontrol1_bytecode_parent, pcontrol1_nonempty_bytecode_parent and the bytecode kill-lines build; Trust prints propext, Classical.choice, Quot.sound plus one <theorem>._native.native_decide.ax_1_1 for each bytecode theorem",
        "prerequisite": "The first target builds EVMYulLean's keccak/sha2 FFI shared objects. native_decide runs the compiled interpreter, which needs them; lakefile.lean passes both via --load-dynlib, libleanffi.so first."
      },
      "summary": "The control plane of both predeploys on the pinned runtimes, now with queue interaction: msg.sender alone decides the fee quote vs system subroutine; the quote folds count at TARGET (8/2) and leaves the queue untouched; a paid submission bumps only SLOT_COUNT; every system call\u2014even with 2 or 65 deposits / 2 or 17 exits queued\u2014resets SLOT_COUNT, rewrites SLOT_EXCESS to max(0, excess+count-target) and drains the FIFO (returning 368/11776 deposits, 136/1088 exits) or to INHIBITOR on nonempty calldata; an empty system call from the inhibited image clears to 0 and still drains; while inhibited, user calls revert but system calls still succeed and still drain.",
      "abstract": {
        "status": "CHECKED",
        "theorem": "Eip8282.Audit.Guarantees.PControl1.empty_updates_excess"
      },
      "evm": {
        "status": "CHECKED",
        "scope": "CONCRETE_TRACES",
        "theorem": "Eip8282.Audit.Guarantees.PControl1.pcontrol1_nonempty_bytecode_parent",
        "executes": "EvmYul.EVM.Xi (EVMYulLean f7e4ee0dc8f8d5265ce822a937ab5be771f182e9) over the pinned runtime bytes of pinned/bytecode/builder_deposits/main.hex and builder_exits/main.hex, driven by Eip8282.Audit.EvmRunner. System calls go through EvmRunner.runDepositSystem / runExitSystem, which differ from the user runners only in msg.sender (SYSTEM_ADDR 0xfffffffffffffffffffffffffffffffffffffffe) and zero value.",
        "world": "Two accounts (predeploy plus caller); caller 0x1234 and SYSTEM_ADDR; gas 30000000; fuel 80000 (300000 for 65-deposit over-cap). Storage families: empty-queue control (excess in {0,5,100,112,120,250,300,2^256-1}, count in {0,1,2,3,4,5,7,8,9,17,20,30,40}, HEAD=TAIL=0) plus nonempty-queue control (excess 100 count 5 HEAD 0 TAIL 2/17/65 with distinctive 6-slot deposit / 3-slot exit records at QUEUE_OFFSET=4, and the inhibited variants with excess=2^256-1). Excess still folds 100+5-8=97 / 100+5-2=103 even though the return buffer is 368/11776/136/1088 and HEAD advances to 0/64/16.",
        "kill_line": {
          "module": "Eip8282.Tests.PControl1Mutant",
          "theorem": "Eip8282.Tests.PControl1Mutant.wave5_mutant_refutes_nonempty_parent",
          "mutation": "Wave-5 adds two single-byte cuts on the system-side TARGET_PER_BLOCK, each fed to the same nonemptyControlFacts the new parent is registered against: builder_deposits runtime byte 571, PUSH1 8 -> 9 (deposit compute_excess), and builder_exits runtime byte 401, PUSH1 2 -> 3 (exit compute_excess). The gate EQ at 22 (CALLER vs SYSTEM_ADDR) already refutes the empty parent and also the nonempty one, but the new cuts are the load-bearing ones for the nonempty excess fold: with the deposit cut, depositQueue 2 no longer folds 100+5-8 to 97 but to 96 and depositQueue 65 still returns 11776 but excess is 96; with the exit cut, exitQueue 2 folds to 102 not 103 and exitQueue 17 still returns 1088 but excess is 102. The user-side bump_excess at 83/82, the LOG0 sizes, and the drain-only caps are left alone.",
          "effect": "nonemptyControlFacts wave5TargetMutatedDeposit exitRuntime = false and nonemptyControlFacts depositRuntime wave5TargetMutatedExit = false, while gateMutatedDeposit also gives false. Concretely wave5_target_shifts_nonempty_excess: depositQueue 2 system call stores 96 not 97, exitQueue 2 stores 102 not 103, so depositNonemptyUnderCapFact and exitNonemptyUnderCapFact are false. The mutations are to bytecode bytes, not to any model function, and each mutant is fed to the same nonemptyControlFacts the new parent is registered against.",
          "independence": "Eip8282.Tests.PControl1Mutant.wave5_mutants_leave_psubmit1_intact proves both new system-target cuts leave Eip8282.Audit.Guarantees.PSubmit1.submitFacts true. P-SUBMIT-1 never calls as SYSTEM_ADDR and never reaches compute_excess. The nonempty parent also leaves the empty-queue parent true on the pinned bytes, and gateMutatedDeposit still refutes the nonempty parent, so the Wave-5 parent is not a tautology and is false if the queue were empty (return size 368/1088 vs 0, HEAD 64/16 vs 0). P-DRAIN-1's FIFO and amount recoding are separate and remain true on the pinned bytes; the Wave-5 kill-line is not a restatement of a sibling."
        }
      },
      "verity": {
        "status": "OPEN",
        "theorem": null
      },
      "fidelity": {
        "covered": [
          "abstract: targets 8 / 2; initial deposit enabled / exit inhibited; read-only fee getter; count increment and system reset; INHIBITOR set/clear/update; inhibit users, not system",
          "bytecode empty-queue: caller gate (SYSTEM_ADDR vs 0x1234, 100 5 0 0 -> 97 0 0 0 vs 32-byte quote); count reset over 0/1/5/8/40; excess max(0,excess+count-target) straddling target on both sides and exactly on it; INHIBITOR set/clear/reversible; fee quote folded at target and readonly; submission bumps only count",
          "bytecode nonempty-queue (Wave 5): fee quote with 2 queued records leaves HEAD 0 TAIL 2 and still folds at target; system call with 2 queued deposits returns 368 bytes, zeroes HEAD/TAIL, excess 97; with 65 queued deposits returns 11776 bytes, HEAD 64 TAIL 65, excess 97; with 2 queued exits returns 136 bytes, excess 103; with 17 queued exits returns 1088 bytes, HEAD 16 TAIL 17, excess 103; INHIBITOR with nonempty queue still stores 2^256-1 and still drains (368/136), and re-enable from inhibited with nonempty queue still clears to 0 and still drains"
        ],
        "missing": [
          "the bytecode facts are concrete traces at a finite family of storage images; they are not universally quantified",
          "fake_exponential pinned only relationally (fold at target plus specific values); not proved equal to EIP formula for all excess",
          "initial_gating remains abstract for the exits ctor (INHIBITOR store in init code not executed)",
          "no equivalence proof between Model.systemCall and the bytecode layer",
          "pre-fork multi-block count accumulation and upgrade orchestration",
          "Verity Contract.run"
        ]
      },
      "classification": {
        "kind": "PARTIAL",
        "work": "Wave-5 nonempty-queue parent is checked. Do not read pcontrol1_nonempty_bytecode_parent as universally quantified; it is a finite-trace, mutation-discriminating suite that now covers the control/drain interaction the empty-queue parent left open. Both per-kind TARGETs (8 and 2) are on the kill-line via the system compute_excess bytes 571 and 401."
      },
      "next_gate": "Generalise from the fixed nonempty queue depths (2,17,65) and the fixed excess family to symbolic/inductive coverage; execute the exits ctor to close initial_gating on bytecode.",
      "roadmap_priority": "NOW"
    }
  ]
}
