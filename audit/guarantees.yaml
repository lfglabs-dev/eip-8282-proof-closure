{
  "schema": "eip-8282-assurance-contract-v1",
  "authority": "Lean theorem statements and proofs are authoritative; metadata classifies but never closes evidence.",
  "objective": "Prove three public EIP-8282 predeploy guarantees. Load-bearing evidence is the registered Lean parent of each ID: P-SUBMIT-1, P-CONTROL-1, and P-DRAIN-1 are CFG-level ∀ under WellFormed/CallHyp plus kill-line Ξ traces. An abstract Lean model is supporting evidence only. Deployment provenance is OPEN.",
  "guarantees": [
    {
      "id": "P-SUBMIT-1",
      "parent": "Eip8282.Audit.Guarantees.PSubmit1.psubmit1_forall_parent",
      "parent_note": "The registered parent is the CFG-level ∀ theorem under WellFormed/CallHyp, conjoined with the Wave-6 psubmit1_bytecode_parent traces that the kill-line still refutes. The abstract-model theorem below is supporting evidence, not a substitute: per audit/WAVE0.md, abstract CHECKED does not stand in for bytecode CHECKED. F4 left A-ABSTRACT-TX open; this parent is not Ξ ↔ Model.",
      "assumptions": [
        "A-ABSTRACT-TX",
        "A-REACHABLE",
        "A-CLASSICAL-CHOICE",
        "A-NATIVE-DECIDE",
        "A-EVM-WORLD",
        "A-PINNED-SOURCE"
      ],
      "reproduction": {
        "command": "lake build EvmYul.FFI.ffi:dynlib && lake build Eip8282.Audit.Guarantees.PSubmit1 Eip8282.Tests.PSubmit1Mutant Eip8282.Audit.Trust",
        "expected": "psubmit1_forall_parent, psubmit1_bytecode_parent and the bytecode kill-line build; Trust prints propext, Classical.choice, Quot.sound for the ∀ conjuncts and for the four D_J jumpdest tables and their validJumps bridges, plus one <theorem>._native.native_decide.ax_1_1 for each kept bytecode trace theorem (parent traces, Wave-1 getter mutant, Wave-4 LOG0 mutant, Wave-6 underpay mutant, and independence)",
        "prerequisite": "The first target builds EVMYulLean's keccak/sha2 FFI shared objects. native_decide runs the compiled interpreter, which needs them; lakefile.lean passes both via --load-dynlib, libleanffi.so first."
      },
      "summary": "While enabled, a non-system write succeeds exactly on well-formed paid input; a success retains msg.value, appends exactly one authentic record (deposit calldata verbatim, or msg.sender||pubkey for an exit), and emits one anonymous LOG0 of that record (184-byte deposit calldata / 68-byte exit source||pubkey); every revert leaves state unchanged. Under CallHyp (WellFormed storage, gas ≥ 30M, fuel ≥ 80000, user caller) this is a CFG-level ∀: bad calldatasize, underpay parameterized by the quoted fee, inhibitor, value-on-getter, min-amount, and stake all JUMPI @revert before the first SSTORE/LOG0; a paying 184-byte deposit appends six words at tail*6 and LOG0s the calldata; a paying 48-byte exit writes CALLER then pubkey and LOG0s the 68-byte msg.sender||pubkey; the empty-calldata getter returns 32 bytes and its completing CFG run records an empty SSTORE overlay, so post-storage slots 0–3 read through that overlay equal the pre-state; fakeExponential is the Model go/asmLoop recurrence for all excess (CFG fragment, not Ξ). The Wave-6 submitFacts traces remain as the kill-line witness.",
      "abstract": {
        "status": "CHECKED",
        "theorem": "Eip8282.Audit.Guarantees.PSubmit1.success_count_and_balance"
      },
      "evm": {
        "status": "CHECKED",
        "scope": "WELL_FORMED_FORALL",
        "theorem": "Eip8282.Audit.Guarantees.PSubmit1.psubmit1_forall_parent",
        "executes": "CFG stepper on pinned user-path prefixes / handle_input / getter suffix of pinned/bytecode/builder_deposits/main.hex and builder_exits/main.hex (S1–S3), plus Model.fakeExponential induction (S4). Kill-line traces still run EvmYul.EVM.Xi (EVMYulLean 0ff72b2db35302305fccb1d4f42b480434b77bfe) via Eip8282.Audit.EvmRunner. Not a proof that Ξ computes fakeExponential or that Ξ agrees with Model.userCall.",
        "world": "CallHyp: WellFormed packed queue (slots 0–3, head ≤ tail), gas ≥ 30000000, interpreter fuel ≥ 80000, caller ≠ SYSTEM_ADDR. Quote is a stack parameter on revert/append fragments. Kill-line traces keep the two-account synthetic world and two reachable-shaped storage images (excess=100 count=5 head=7 tail=9 quoting 357/427, and excess=50 count=3 head=2 tail=6 quoting 18/19), plus inhibited variants with SLOT_EXCESS = 2^256-1.",
        "kill_line": {
          "module": "Eip8282.Tests.PSubmit1Mutant",
          "theorem": "Eip8282.Tests.PSubmit1Mutant.mutant_refutes_parent",
          "mutation": "three single-byte cuts, each fed to the same submitFacts the ∀ parent still contains via psubmit1_bytecode_parent: builder_deposits runtime byte 158, RETURN (0xf3) -> REVERT (0xfd), the instruction that ends the fee-getter path (CFG: suffix local 30 / prefix PC 158); builder_deposits runtime byte 274, PUSH1 operand 184 -> 0, the RECORD_SIZE that sizes the user-path LOG0 (CFG: handle_input relative 114, PUSH1 184 at runtime 273); and builder_deposits runtime byte 161, CALLVALUE (0x34) -> GAS (0x5a), the msg.value the handle_input fee check compares against the quote (CFG: handle_input relative 2 / prefix PC 161). The getter-path CALLVALUE at 148, the stake-check CALLVALUE at 198, the LT at 162, the calldatacopy size at offset 269, the LOG0 opcode at offset 276, the exit fee-check CALLVALUE at 159, the exit user-path RECORD_SIZE at offset 215, and P-DRAIN-1's system RETURN size at exit offset 450 are left alone.",
          "effect": "submitFacts mutatedDepositRuntime exitRuntime = false, and the fee getter reverts instead of succeeding. submitFacts logSizeMutatedDeposit exitRuntime = false: the paid deposit still appends the six calldata words, but LOG0 data size is 0 instead of 184, so depositPaidLogFact fails. submitFacts underpayMutatedDeposit exitRuntime = false: a well-formed 184-byte deposit whose msg.value is liveDepositFee-1 (and altDepositFee-1 on the second image) now succeeds and writes SLOT_COUNT/SLOT_TAIL, so depositUnderpayFact and altDepositUnderpayFact fail. Because psubmit1_forall_parent includes psubmit1_bytecode_parent, which includes submitFacts depositRuntime exitRuntime = true, each mutant makes the registered parent false of that bytecode. The mutations are to bytecode bytes, not to any model function, and each mutant is fed to the same submitFacts the parent is registered against.",
          "independence": "Eip8282.Tests.PSubmit1Mutant.log_mutant_leaves_siblings_intact proves the LOG0-size mutant leaves Eip8282.Audit.Guarantees.PDrain1.drainFacts and Eip8282.Audit.Guarantees.PControl1.controlFacts true. Eip8282.Tests.PSubmit1Mutant.underpay_mutant_leaves_siblings_intact proves the fee-check CALLVALUE mutant leaves the same sibling parents true. P-DRAIN-1 only calls from SYSTEM_ADDR; P-CONTROL-1's paid submissions use payment strictly above the quote and do not observe underpay. The new conjuncts are not a restatement of a sibling guarantee."
        }
      },
      "fidelity": {
        "covered": [
          "abstract: revert leaves persistent state unchanged; a successful write appends one record and retains full value; exit sourceAddress equals caller; inhibited user calls revert",
          "CFG ∀: every user-path JUMPI @revert sits before the first SSTORE/LOG0 on both runtimes",
          "CFG ∀: inhibitor, bad calldatasize (not 0 and not 184/48), value-on-getter, underpay (value < quotedFee, quote a parameter), deposit min-amount, and deposit stake all land on *.revert before writes",
          "CFG ∀: paying 184-byte deposit appends the six CALLDATALOAD words at QUEUE_OFFSET + tail*6 and LOG0s the 184-byte calldata",
          "CFG ∀: paying 48-byte exit writes CALLER then the two pubkey words at QUEUE_OFFSET + tail*3 and LOG0s sender||pubkey (68 bytes)",
          "CFG ∀: empty calldata, value 0, user path: 32-byte RETURN; the completing getter run's recorded SSTORE list is empty, so post-storage slots 0–3 read through that list equal the well-formed pre-state for every excess/count; bump_excess is stack-only",
          "algebraic ∀: Model.fakeExponential 1 (foldedExcess excess count TARGET) 17 equals go/asmLoop for all excess (user-quote guard excess ≠ INHIBITOR); CFG fragment of the loop body, not Ξ",
          "bytecode traces (kill-line): empty calldata with value=0 returns exactly 32 bytes and leaves slots 0-3 unchanged, at the fake_exponential values 357 (deposit) and 427 (exit)",
          "bytecode traces: empty calldata with value!=0 reverts, so no storage write survives",
          "bytecode traces: a paid 184-byte deposit appends the six CALLDATALOAD words verbatim at QUEUE_OFFSET + tail*6 and bumps only SLOT_COUNT and SLOT_TAIL",
          "bytecode traces: a paid 48-byte exit writes msg.sender into the first appended slot, then the two pubkey words, at QUEUE_OFFSET + tail*3",
          "bytecode traces: with SLOT_EXCESS = INHIBITOR, both the getter and the paid write path revert on both predeploys",
          "bytecode traces: a successful paid deposit emits one anonymous LOG0 (0 topics) whose data is the 184-byte calldata; a successful paid exit emits one anonymous LOG0 whose 68-byte data is msg.sender || pubkey",
          "bytecode traces: a well-formed 184-byte deposit / 48-byte exit whose msg.value is strictly below the quoted fee (356 vs 357 on deposits, 426 vs 427 on exits) reverts; Ξ returns no account map, so slots 0-3 and the live-tail queue words are not observable as writes",
          "bytecode traces: the same getter, paid append, LOG0, inhibited revert, and underpay freeze hold at a second reachable-shaped image (excess=50 count=3 head=2 tail=6; quotes 18 deposits / 19 exits)"
        ],
        "missing": [
          "no equivalence proof between Eip8282.Audit.Model.userCall and EvmYul.EVM.Xi; the ∀ layer is CFG-direct (A-ABSTRACT-TX remains)",
          "the ∀ does not claim Ξ computes fakeExponential; S4 is algebraic plus a loop-body opcode fragment",
          "the system-call drain path is not exercised here (see P-DRAIN-1)",
          "constructor/init bytecode and deployed-address provenance"
        ]
      },
      "classification": {
        "kind": "PARTIAL",
        "work": "CFG-level ∀ under WellFormed/CallHyp for the user-path revert, append+LOG0, getter readonly, and algebraic fakeExponential, with the Wave-6 Ξ traces kept as the kill-line. The revert conjuncts are stated over EvmYul.EVM.D_J depositRuntime 0 / D_J exitRuntime 0 — Ξ's own jumpdest analysis of the pinned image, kernel-checked by decide +kernel — not over hand-written jumpdest arrays. Do not read psubmit1_forall_parent as Ξ ↔ Model or as a universally quantified interpreter trace."
      },
      "next_gate": "Close A-ABSTRACT-TX by proving Ξ agrees with the CFG stepper / Model.userCall on well-formed user calls.",
      "roadmap_priority": "NOW"
    },
    {
      "id": "P-DRAIN-1",
      "parent": "Eip8282.Audit.Guarantees.PDrain1.pdrain1_forall_parent",
      "parent_note": "The registered parent is the CFG-level ∀ theorem under WellFormed/CallHyp (system path isUser = false, gas ≥ 30M), conjoined with the Wave-6 pdrain1_bytecode_parent traces that the kill-line still refutes. The abstract-model theorem below is supporting evidence, not a substitute: per audit/WAVE0.md, abstract CHECKED does not stand in for bytecode CHECKED. F4 left A-ABSTRACT-TX open; this parent is not Ξ ↔ Model and does not claim Ξ computes FIFO for every excess.",
      "assumptions": [
        "A-ABSTRACT-TX",
        "A-REACHABLE",
        "A-CLASSICAL-CHOICE",
        "A-NATIVE-DECIDE",
        "A-EVM-WORLD",
        "A-PINNED-SOURCE"
      ],
      "reproduction": {
        "command": "lake build EvmYul.FFI.ffi:dynlib && lake build Eip8282.Audit.Guarantees.PDrain1 Eip8282.Tests.PDrain1Mutant Eip8282.Audit.Trust",
        "expected": "pdrain1_forall_parent, pdrain1_bytecode_parent and the bytecode kill-line build; Trust prints propext, Classical.choice, Quot.sound for the ∀ conjuncts and for the four D_J jumpdest tables and their validJumps bridges, plus one <theorem>._native.native_decide.ax_1_1 for each kept bytecode trace theorem",
        "prerequisite": "The first target builds EVMYulLean's keccak/sha2 FFI shared objects. native_decide runs the compiled interpreter, which needs them; lakefile.lean passes both via --load-dynlib, libleanffi.so first."
      },
      "summary": "Only the system-call path consumes queued records. Under CallHyp (WellFormed storage, gas ≥ 30M, fuel ≥ 80000, SYSTEM_ADDR caller) this is a CFG-level ∀: system SSTORE keys ⊆ {SLOT_EXCESS, SLOT_COUNT, QUEUE_HEAD, QUEUE_TAIL} so every slot n ≥ 4 is unchanged; n = min(tail-head, capOf) with wrap-free SUB/ADD, the oldest n packed items, full drain pointers (0,0) and partial HEAD+=n TAIL unchanged, caps 64/16; deposit amount BE storage → LE return ∀ drained index; a user fee quote does not move HEAD/TAIL. The Wave-6 drainFacts traces remain as the kill-line witness. Not a reduction of Ξ/X.",
      "abstract": {
        "status": "CHECKED",
        "theorem": "Eip8282.Audit.Guarantees.PDrain1.fifo_bounded"
      },
      "evm": {
        "status": "CHECKED",
        "scope": "CFG_FORALL",
        "theorem": "Eip8282.Audit.Guarantees.PDrain1.pdrain1_forall_parent",
        "executes": "CFG stepper on pinned system-path clamp / update_head / store_excess / accum_loop fragments of pinned/bytecode/builder_deposits/main.hex and builder_exits/main.hex (D1–D3), plus packed-queue algebra under WellFormed. Kill-line traces still run EvmYul.EVM.Xi (EVMYulLean 0ff72b2db35302305fccb1d4f42b480434b77bfe) via Eip8282.Audit.EvmRunner.runDepositSystem / runExitSystem. Not a proof that Ξ computes FIFO or that Ξ agrees with Model.systemCall.",
        "world": "CallHyp: WellFormed packed queue (slots 0–3, head ≤ tail, tail < 2^64), gas ≥ 30000000, interpreter fuel ≥ 80000, caller = SYSTEM_ADDR (isUser = false) for D1–D3 system lemmas; user CallHyp for the fee-quote pointer lemma. Kill-line traces keep the two-account synthetic world and FIFO images (excess=100 count=5 HEAD=0 TAIL in {0,1,2,17} exits / {0,1,2,65} deposits).",
        "kill_line": {
          "module": "Eip8282.Tests.PDrain1Mutant",
          "theorem": "Eip8282.Tests.PDrain1Mutant.mutant_refutes_parent",
          "mutation": "six single-byte cuts, each fed to the same drainFacts the ∀ parent still contains via pdrain1_bytecode_parent: builder_exits runtime byte 244, PUSH1 operand 16 -> 8, the MAX_PER_BLOCK clamp (CFG: exitClamp relative 18 / read_requests+19); builder_exits runtime byte 450, PUSH1 operand 68 -> 64, the RECORD_SIZE multiplier of the system RETURN (CFG: store_excess relative 8); builder_deposits runtime byte 304, PUSH1 operand 64 -> 32, the MAX_PER_BLOCK clamp (CFG: depositClamp relative 19 / read_requests+20); builder_deposits runtime byte 483, PUSH1 operand 2 -> 9, the QUEUE_HEAD slot of the deposit partial-drain SSTORE (CFG: update_head+12), retargeted onto QUEUE_OFFSET+5; builder_deposits runtime byte 483, PUSH1 operand 2 -> 196, the same QUEUE_HEAD slot retargeted onto QUEUE_OFFSET+6*32; and builder_exits runtime byte 313, PUSH1 operand 2 -> 25, the exit partial-drain QUEUE_HEAD slot (CFG: update_head+12), retargeted onto QUEUE_OFFSET+3*7. The exit comparison immediate at offset 237, the deposit comparison immediate at offset 296, the user-path LOG0 RECORD_SIZE at offset 215, and the empty-queue reset PUSH1 QUEUE_HEAD operands at deposit offset 494 and exit offset 324 are left alone.",
          "effect": "drainFacts depositRuntime capMutatedExit = false, drainFacts depositRuntime recSizeMutatedExit = false, drainFacts capMutatedDeposit exitRuntime = false, drainFacts headSlotMutatedDeposit exitRuntime = false, drainFacts headSlotMutatedDepositItem32 exitRuntime = false, and drainFacts depositRuntime headSlotMutatedExitItem7 = false. With the exit-cap cut, seventeen queued exits return 8*68=544 bytes and QUEUE_HEAD advances to 8 instead of 16, while the under-cap two-record drain is untouched (cap_mutant_halves_the_over_cap_drain). With the record-size cut, the drain loop still walks the same records but the system RETURN reports n*64 bytes, so the two-record drain is 128 instead of 136 and the over-cap drain is 1024 instead of 1088 (rec_size_mutant_shrinks_the_return). With the deposit-cap cut, sixty-five queued deposits return 32*184=5888 bytes and QUEUE_HEAD advances to 32 instead of 64 (deposit_cap_mutant_halves_the_over_cap_drain). With the Wave-3 head-slot cut, sixty-five queued deposits still return 11776 bytes but the advanced head value 64 is written into slot 9 instead of QUEUE_HEAD (head_slot_mutant_overwrites_a_drained_word). With the Wave-6 deposit head-slot cut, that same 64 is written into slot 196 so staleDepositPk1Is 32 fails. With the Wave-6 exit head-slot cut, the new head 16 is written into slot 25 so staleExitSrcIs 7 fails. Because pdrain1_forall_parent includes pdrain1_bytecode_parent, which includes drainFacts depositRuntime exitRuntime = true, each mutant makes the registered parent false of that bytecode. The mutations are to bytecode bytes, not to any model function.",
          "independence": "Eip8282.Tests.PDrain1Mutant.drain_mutants_leave_siblings_intact proves all six mutants leave Eip8282.Audit.Guarantees.PSubmit1.submitFacts and Eip8282.Audit.Guarantees.PControl1.controlFacts true. P-SUBMIT-1 never calls from SYSTEM_ADDR. P-CONTROL-1's empty-queue controlFacts hold QUEUE_HEAD=QUEUE_TAIL=0, so the cap clamp is never taken, the partial-drain head stores are never taken, and 0*RECORD_SIZE is still 0: the P-DRAIN-1 parent is not a restatement of a sibling guarantee. P-SUBMIT-1 / P-CONTROL-1 registered parents are not changed."
        }
      },
      "fidelity": {
        "covered": [
          "abstract: system call never reverts; FIFO take/drop with per-kind cap; full drain empties the logical queue; deposit amount LE conversion; exit records unchanged",
          "CFG ∀: system SSTORE keys ⊆ {0,1,2,3}; ∀ n ≥ 4 a system CFG store list leaves that slot equal to the pre-image (CallHyp isUser = false)",
          "CFG ∀: n = min(tail-head, capOf) with wrap-free UInt256 SUB/ADD under WellFormed; accum_loop iterates n times; oldest window [head, head+n); full drain stores (0,0); partial HEAD+=n TAIL unchanged; caps 64 deposits / 16 exits at bytes 304/244",
          "CFG ∀: deposit amount field bytes 80–87 of each drained index are the little-endian encoding of the big-endian packed amount; exit records are 20-byte BE source then 48-byte pubkey with no recode",
          "CFG ∀: user empty-calldata / value-0 fee quote lands on the user path, getter bytes contain no SSTORE, HEAD/TAIL window unchanged",
          "bytecode traces (kill-line): empty exit queue, SYSTEM_ADDR, empty calldata: success, 0-byte return, SLOT_EXCESS 100 -> 103, SLOT_COUNT 5 -> 0, both queue pointers stay 0",
          "bytecode traces: two queued exits return 136 bytes in FIFO order (record 0 then record 1, 68 bytes each) and a full drain zeroes QUEUE_HEAD and QUEUE_TAIL",
          "bytecode traces: seventeen queued exits return exactly 16 records (1088 bytes), QUEUE_HEAD advances to 16, QUEUE_TAIL stays 17; all three words of drained items 0, 7 and 15 remain in storage",
          "bytecode traces: a user fee-getter on a two-record exit queue returns 32 bytes and leaves head=0 tail=2, so only SYSTEM_ADDR consumes the queue",
          "bytecode traces: one queued deposit returns 184 bytes with the amount field recoded from big-endian 0x0102030405060708 to little-endian 08 07 06 05 04 03 02 01, and a full drain zeroes both pointers",
          "bytecode traces: two queued deposits return 368 bytes with amounts in FIFO order",
          "bytecode traces: empty deposit queue, SYSTEM_ADDR, empty calldata: success, 0-byte return, SLOT_EXCESS 100 -> 97, SLOT_COUNT 5 -> 0, both queue pointers stay 0",
          "bytecode traces: sixty-five queued deposits return exactly 64 records (11776 bytes), QUEUE_HEAD advances to 64, QUEUE_TAIL stays 65; leftover words of items 0/1/32/63/64 remain in storage"
        ],
        "missing": [
          "no equivalence proof between Eip8282.Audit.Model.systemCall and EvmYul.EVM.Xi; the ∀ layer is CFG-direct (A-ABSTRACT-TX remains)",
          "the ∀ does not claim Ξ computes FIFO for every excess or every queue image; D2 is packed-queue algebra plus clamp/pointer opcode fragments",
          "30_000_000 gas bound as an EVM resource fact (hypothesis on CallHyp, not a proved bound on X)",
          "EIP-7685 request-type prefixing",
          "constructor/init bytecode and deployed-address provenance"
        ]
      },
      "classification": {
        "kind": "PARTIAL",
        "work": "CFG-level ∀ under WellFormed/CallHyp for system SSTORE footprint, FIFO count/pointers (caps 64/16), and deposit BE→LE encode, with the Wave-6 Ξ traces kept as the kill-line. The leading conjunct is Eip8282.Audit.Step.validJumps_are_Xi_tables, which pins all four jumpdest tables to D_J of the pinned runtime and ctor images (decide +kernel, no native_decide); the P-DRAIN CFG lemmas step only against the two runtime tables, so the conjunct constrains those. The two init-table equalities are independent facts carried alongside, and P-DRAIN-1 has no ctor fragment. Do not read pdrain1_forall_parent as Ξ ↔ Model or as a universally quantified interpreter trace."
      },
      "next_gate": "Close A-ABSTRACT-TX by proving Ξ agrees with the CFG stepper / Model.systemCall on well-formed system drains.",
      "roadmap_priority": "NOW"
    },
    {
      "id": "P-CONTROL-1",
      "parent": "Eip8282.Audit.Guarantees.PControl1.pcontrol1_forall_parent",
      "parent_note": "The registered parent is the CFG-level ∀ theorem under WellFormed/CallHyp (gas ≥ 30M, caller class), conjoined with the Wave-1 pcontrol1_bytecode_parent, Wave-5 pcontrol1_nonempty_bytecode_parent, and fold-discrimination traces that the kill-line still refutes. The fold-discrimination conjunct (pcontrol1_fold_discriminate_parent) asserts that the user-path bump_excess fold (excess + max(0, count−TARGET)) and the system-path update_excess fold (max(0, excess+count−TARGET)) produce different values on both runtimes. The abstract-model theorem below is supporting evidence, not a substitute: per audit/WAVE0.md, abstract CHECKED does not stand in for bytecode CHECKED. F4 left A-ABSTRACT-TX open; this parent is not Ξ ↔ Model.",
      "assumptions": [
        "A-ABSTRACT-TX",
        "A-REACHABLE",
        "A-CLASSICAL-CHOICE",
        "A-NATIVE-DECIDE",
        "A-EVM-WORLD",
        "A-PINNED-SOURCE"
      ],
      "reproduction": {
        "command": "lake build EvmYul.FFI.ffi:dynlib && lake build Eip8282.Audit.Guarantees.PControl1 Eip8282.Tests.PControl1Mutant Eip8282.Audit.Trust",
        "expected": "pcontrol1_forall_parent, pcontrol1_bytecode_parent, pcontrol1_nonempty_bytecode_parent, pcontrol1_fold_discriminate_parent, and the bytecode kill-lines build; Trust prints propext, Classical.choice, Quot.sound for the ∀ conjuncts and for the four D_J jumpdest tables and their validJumps bridges, plus one <theorem>._native.native_decide.ax_1_1 for each kept bytecode trace theorem (empty-queue parent, nonempty-queue parent, fold discrimination, Wave-1 mutants, Wave-5 mutants, fold-equating mutants, and independence)",
        "prerequisite": "The first target builds EVMYulLean's keccak/sha2 FFI shared objects. native_decide runs the compiled interpreter, which needs them; lakefile.lean passes both via --load-dynlib, libleanffi.so first."
      },
      "summary": "The control plane of both predeploys: msg.sender alone decides the fee-quote vs system subroutine; a paid user wraps SLOT_COUNT += 1 and leaves excess; every system call resets SLOT_COUNT and rewrites SLOT_EXCESS to INHIBITOR on nonempty calldata, 0 if already inhibited, else max(0, excess+count-TARGET) for TARGET 8 (deposits) and 2 (exits). The user-path bump_excess fold (excess + max(0, count−TARGET)) and the system-path update_excess fold (max(0, excess+count−TARGET)) are distinct computations: at (100, 5, 8) they give 100 vs 97, at (100, 1, 2) they give 100 vs 99. Queue length is unused. Exit init stores INHIBITOR at slot 0 then returns runtime; deposit init does not SSTORE. Under CallHyp (WellFormed storage, gas ≥ 30M, caller class) this is a CFG-level ∀ (C1 gate, C2 excess, C3 count, C4 ctor). The Wave-1 controlFacts, Wave-5 nonemptyControlFacts, and fold-discrimination traces remain as the kill-line witness.",
      "abstract": {
        "status": "CHECKED",
        "theorem": "Eip8282.Audit.Guarantees.PControl1.empty_updates_excess"
      },
      "evm": {
        "status": "CHECKED",
        "scope": "CFG_FORALL",
        "theorem": "Eip8282.Audit.Guarantees.PControl1.pcontrol1_forall_parent",
        "executes": "CFG stepper on the pinned opening CALLER/EQ/JUMPI gate (C1), the system update_excess … SLOT_EXCESS SSTORE block (C2), the paid-user SLOT_COUNT increment and system store_excess count reset (C3), and the deposit/exit init preambles (C4) of pinned/bytecode/builder_{deposits,exits}/{ctor,main}.hex. Kill-line traces still run EvmYul.EVM.Xi (EVMYulLean 0ff72b2db35302305fccb1d4f42b480434b77bfe) via Eip8282.Audit.EvmRunner. Not a proof that Ξ agrees with Model.userCall/systemCall.",
        "world": "CallHyp: WellFormed packed queue (slots 0–3, head ≤ tail), gas ≥ 30000000, caller class (SYSTEM_ADDR vs not). C2 additionally needs the UInt256 sum-no-wrap hypothesis on the empty non-inhibited fold. Ctor CFG is the init preamble under campaign gas, not CREATE. Kill-line traces keep the two-account synthetic world: empty-queue control (excess in {0,5,100,112,120,250,300,2^256-1}, count in {0,1,2,3,4,5,7,8,9,17,20,30,40}, HEAD=TAIL=0) plus nonempty-queue control (excess 100 count 5 HEAD 0 TAIL 2/17/65) and inhibited variants.",
        "kill_line": {
          "module": "Eip8282.Tests.PControl1Mutant",
          "theorem": "Eip8282.Tests.PControl1Mutant.wave5_mutant_refutes_nonempty_parent",
          "mutation": "three standard single-byte cuts plus two fold-equating cuts, each fed to the same controlFacts / nonemptyControlFacts / fold-discrimination facts the ∀ parent still contains: builder_deposits runtime byte 22, EQ (0x14) -> LT (0x10), the CALLER vs SYSTEM_ADDR gate (CFG: gateEqPc); builder_deposits runtime byte 571, PUSH1 operand 8 -> 9, the system compute_excess TARGET (CFG: update_excess local 70 / blockBase+71); builder_exits runtime byte 401, PUSH1 operand 2 -> 3, the exit system TARGET (Wave 5). Fold-equating cuts: builder_deposits byte 571 PUSH1 operand 8 -> 5 (equates bump_excess and update_excess at (100,5,8): both give 100); builder_exits byte 401 PUSH1 operand 2 -> 1 (equates at (100,1,2): both give 100). mutant_refutes_parent covers EQ@22 and TARGET 8@571 against controlFacts; wave5_mutant_refutes_nonempty_parent covers TARGET 8@571, TARGET 2@401, and EQ@22 against nonemptyControlFacts; fold_equating_mutant_refutes_discrimination covers 571: 8→5 and 401: 2→1 against the fold-discrimination facts; existing_kill_line_refutes_fold_discrimination covers 571: 8→9 and 401: 2→3. The user-side bump_excess at 83/82, the LOG0 sizes, and the drain-only caps are left alone.",
          "effect": "controlFacts gateMutatedDeposit exitRuntime = false, controlFacts depositRuntime gateMutatedExit = false, and controlFacts targetMutatedDeposit exitRuntime = false. nonemptyControlFacts wave5TargetMutatedDeposit exitRuntime = false, nonemptyControlFacts depositRuntime wave5TargetMutatedExit = false, and nonemptyControlFacts gateMutatedDeposit exitRuntime = false. depositFoldDiscriminateFact foldEquatingDeposit = false and exitFoldDiscriminateFact foldEquatingExit = false: the equating cuts make the system store the bump_excess value (100) instead of the update_excess value (97/99), merging the two folds. depositFoldDiscriminateFact targetMutatedDeposit = false and exitFoldDiscriminateFact wave5TargetMutatedExit = false: the standard kill-line also refutes the fold discrimination. Concretely the gate cut answers SYSTEM_ADDR as a user (32-byte quote, count stays 5); the deposit TARGET cut stores excess 96 not 97 on empty and nonempty images; the exit TARGET cut stores 102 not 103; the fold-equating deposit cut stores 100 not 97; the fold-equating exit cut stores 100 not 99. Because pcontrol1_forall_parent includes the bytecode parents and the fold discrimination, each mutant makes the registered parent false of that bytecode. The mutations are to bytecode bytes, not to any model function.",
          "independence": "Eip8282.Tests.PControl1Mutant.control_mutants_leave_psubmit1_intact, wave5_mutants_leave_psubmit1_intact, and fold_equating_mutants_leave_psubmit1_intact prove the EQ@22, TARGET 8/2, and fold-equating cuts leave Eip8282.Audit.Guarantees.PSubmit1.submitFacts true. P-SUBMIT-1 never calls as SYSTEM_ADDR and never reaches compute_excess. P-DRAIN-1's FIFO and amount recoding are separate and remain true on the pinned bytes; the P-CONTROL-1 parent is not a restatement of a sibling. P-SUBMIT-1 / P-DRAIN-1 registered parents are not changed."
        }
      },
      "fidelity": {
        "covered": [
          "abstract: targets 8 / 2; initial deposit enabled / exit inhibited; read-only fee getter; count increment and system reset; INHIBITOR set/clear/update; inhibit users, not system",
          "CFG ∀: CALLER = SYSTEM_ADDR iff the opening EQ/JUMPI lands on read_requests, for both predeploys, every WellFormed storage, and campaign gas (C1)",
          "CFG ∀: nonempty system calldata stores INHIBITOR at slot 0; inhibited+empty stores 0; else max(0, excess+count−TARGET) for TARGET 8 (deposit byte 571) and 2 (exit byte 401); queue length unused (C2)",
          "CFG ∀: paid user wraps SLOT_COUNT += 1 and leaves SLOT_EXCESS; system store_excess writes SLOT_COUNT := 0; ∀ prior count mod 2^256 (C3)",
          "CFG ∀: deposit init does not SSTORE; exit init's first SSTORE is slot 0 := INHIBITOR, then CODECOPY/RETURN of the runtime; initial_gating on those post-images (C4)",
          "bytecode traces (kill-line, empty-queue): caller gate (SYSTEM_ADDR vs 0x1234, 100 5 0 0 -> 97 0 0 0 vs 32-byte quote); count reset over 0/1/5/8/40; excess max(0,excess+count-target) straddling target on both sides and exactly on it; INHIBITOR set/clear/reversible; fee quote folded at target and readonly; submission bumps only count",
          "bytecode traces (kill-line, nonempty-queue Wave 5): fee quote with 2 queued records leaves HEAD 0 TAIL 2 and still folds at target; system call with 2 queued deposits returns 368 bytes, zeroes HEAD/TAIL, excess 97; with 65 queued deposits returns 11776 bytes, HEAD 64 TAIL 65, excess 97; with 2 queued exits returns 136 bytes, excess 103; with 17 queued exits returns 1088 bytes, HEAD 16 TAIL 17, excess 103; INHIBITOR with nonempty queue still stores 2^256-1 and still drains (368/136), and re-enable from inhibited with nonempty queue still clears to 0 and still drains",
          "bytecode traces (fold discrimination): the user-path bump_excess fold (excess + max(0, count−TARGET)) and system-path update_excess fold (max(0, excess+count−TARGET)) produce different values: deposit at (100, 5, 8) gives bump=100 vs update=97, exit at (100, 1, 2) gives bump=100 vs update=99; the quoted fees differ at those excess values because fake_exponential is strictly increasing; fold-equating mutants (deposit 571: 8→5, exit 401: 2→1) merge the two folds by making the system store the bump_excess value"
        ],
        "missing": [
          "no equivalence proof between Eip8282.Audit.Model.userCall/systemCall and EvmYul.EVM.Xi; the ∀ layer is CFG-direct (A-ABSTRACT-TX remains)",
          "the ∀ does not claim Ξ computes the excess fold or count update; C2/C3 are CFG fragments of update_excess / count increment / store_excess",
          "ctor lemmas are CFG prefixes of init bytecode, not Ξ CREATE traces, and not deployed-address provenance",
          "FIFO drain order and amount recoding are P-DRAIN-1, not this parent",
          "pre-fork multi-block count accumulation and upgrade orchestration"
        ]
      },
      "classification": {
        "kind": "PARTIAL",
        "work": "CFG-level ∀ under WellFormed/CallHyp for the caller gate, excess recurrence (targets 8 and 2), count increment/reset, and init-bytecode gating, with the Wave-1 and Wave-5 Ξ traces kept as the kill-line. The leading conjunct is Eip8282.Audit.Step.validJumps_are_Xi_tables, which pins all four jumpdest tables to D_J of the pinned runtime and ctor images (decide +kernel, no native_decide); only the C1/C2/C3 runtime fragments actually step against a jumpdest table, so the conjunct constrains those. The C4 init preambles contain no jump and consume no table: the two init-table equalities are independent facts carried alongside pcontrol1_c4_ctor_forall, not a transport into it. Do not read pcontrol1_forall_parent as Ξ ↔ Model or as a universally quantified interpreter trace."
      },
      "next_gate": "Close A-ABSTRACT-TX by proving Ξ agrees with the CFG stepper / Model.systemCall on well-formed system and user calls.",
      "roadmap_priority": "NOW"
    }
  ]
}
