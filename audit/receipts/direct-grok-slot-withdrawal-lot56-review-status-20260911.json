{
  "delta_base": "0b6c0b5^ = 70c6dd6",
  "delta_head": "0b6c0b5",
  "source_branch": "origin/grok/eip-slot-withdrawal-extraction-20260911",
  "external_author": "Grok agent (Cursor Agent, co-authored by Thomas Marchand)",
  "status": "AWAITING_INDEPENDENT_EXACT_DELTA_REVIEW",
  "recorded_at_utc": "2026-09-11T15:00:00+00:00",
  "reviewer_kind": "fresh_context_claude_subagent_read_only_not_the_author",
  "review_scope": [
    "shuffleRoundBytes_eq r = [r % 256]: phase0:1205 uint_to_bytes 1 = single-byte modulo 256 via simp on uintToBytes.",
    "shuffleRoundBytes_ne: distinct residues mod 256 give distinct singleton lists; simp lifts to list disagreement.",
    "shuffleBucketPreimage_round_ne: full bucket preimages disagree under distinct round residues, via congrArg + List.drop of the seed prefix + simp normalization.",
    "sourceByBucket_rounds_0_1: instance at (0, 1) via decide on the mod-256 disagreement.",
    "source_preimage_uses_round: dropping Uint8(round) shifts the length by 1; refuted via congrArg length + simp.",
    "mem_shuffleRounds_lt: r ∈ shuffleRounds ⇒ r < 90 (List.mem_range under range 90).",
    "mem_shuffleRounds_no_wrap: r ∈ shuffleRounds ⇒ r % 256 = r (via Nat.mod_eq_of_lt using 90 < 256 by decide).",
    "shuffleRounds_round_bytes_inj: distinct rounds in the range yield distinct Uint8 encodings; injectivity via shuffleRoundBytes_ne and _no_wrap.",
    "shuffleRounds_map_bytes_nodup: the 90 archived rounds map to 90 distinct Uint8 bytes; uses nodup_map_on with the injectivity above and List.nodup_range on List.range 90.",
    "Uint8 wrap warning: uint8_round_256_collides_zero — round 256 wraps back to Uint8(0), matching Uint8(0), so an unrestricted 256-round loop would break Nodup. This is why the archived 90 is enough.",
    "echoHeadHash: Hash32Like witness that copies preimage head (first byte after empty seed = the Uint8 round tag). length = 32 by simp; bounded by cons case (mod 256 < 256) + replicate 31 zeros (< 256 via decide).",
    "sourceByBucket_round_ne_echo: on empty seed, rounds 0 and 1 give distinct cached sources under echoHeadHash — via decide.",
    "New #print axioms (12 in slot module, 3 in mutants file — see delta): none introduce new axioms; all decide/rfl/simp/rewriting under standard whitelist.",
    "OPEN scope preserved: SHA256 pivot and swap-bit VALUES still uninterpreted. This lot proves per-round SHA256 preimages are distinct across the archived 90, so a shared cache across rounds is unsound. Not a claim on any hash value.",
    "Every new declaration axiom ∈ {propext, Classical.choice, Quot.sound}."
  ],
  "local_check_by_promoter": {
    "cherry_pick_onto_main": {
      "result": "PASS",
      "spark_branch": "spark/eip-grok-lot56-to-main-20260911"
    },
    "make_check_full": {
      "result": "PASS",
      "collected": "3608 jobs, check ok, ~23s (Lake cache warm)"
    }
  },
  "promotion": "PENDING independent review verdict; PR to main will be opened after CLEAN."
}
