# Public premise audit — 9 September 2026

The three registered parents are proved conditional local Θ/Λ statements for both kinds. Their domains are not yet proved for all protocol-reachable calls. The JSON inventory expands every public domain field and records its consumer, local proof, remaining closure lemma and recommended existing owner.

- Runtime domains require existing owner, actual/apparent value equality, calldata size fit, budget <2^128, four scalar storage bounds, and inhibited-or-natural-numerator≤2892. Code is separately pinned; canonical target and installed-code identity are not public domain fields.
- PDrain additionally requires four physical queue representation fields and retained exit source words <2^160. Initialization and append/drop lemmas provide local preservation, not extraction from all actual journals.
- Initialization has independent context, preimage encoding, fuel, absent target, collision and resource inputs; it **assumes successful actual Lambda settlement**. The low init-execution bounds do not establish code-deposit success or canonical deployment.
- SYSTEM progress instead **proves** actual successful Θ, assuming pinned code, SYSTEM caller, owner, permission, gas≥2,500,000 and fuel≥8,503. It neither schedules the call nor provides those resources. The runtime FIFO/control domain must still be established for the same scheduled call.
- ConcreteHistory proves preservation for supplied contiguous audited Θ transitions. It does not extract external frames, all intermediate nested storage states, or ancestor rollback. A monotone global append budget needs mutual actual event extraction plus Υ refund/block composition.
- Safe-fee preservation depends on an independently justified value ceiling. FundingHistory conservation is substantial local evidence, but transaction Admission, complete credit provenance, initial funding/credit bounds and every nested frame's linkage remain inputs.
- Canonical deployment, code immutability/framing, privileged caller authorization, fork selection and upgrade/inhibition scheduling remain protocol bindings. They cannot be inferred from the raw evaluator.

The source-matched receipt reports only `propext`, `Classical.choice`, and `Quot.sound` for all three parents. Historical finite mutation axioms are separate from correctness parents. No build was run for this audit.

Inventory: 46 entries. Receipt hash comparison: 152 existing listed files checked; mismatches: [].
