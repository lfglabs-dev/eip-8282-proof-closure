# Complete genesis allocation input — proposed reference, unadopted

The cached mainnet asset and loader were re-fetched from immutable execution-specs commit `0cc100eb190b64b23baba72dac0165652eaec252` and compared byte for byte. Both matched. Asset SHA256: `48bb73806f4ed8f0e4f869cb0bc7ba2e1e5724ceb08c9ec079170dfd63042d65` (1,005,416 bytes); loader SHA256: `e22eccebf3404e832245633ff88da4dc304ada33198fbf05d8bab50445af0e3c` (9,369 bytes). Sources: [asset](https://raw.githubusercontent.com/ethereum/execution-specs/0cc100eb190b64b23baba72dac0165652eaec252/src/ethereum/assets/mainnet.json), [loader](https://raw.githubusercontent.com/ethereum/execution-specs/0cc100eb190b64b23baba72dac0165652eaec252/src/ethereum/genesis.py).

The retained `genesis-funding-input-mainnet.json` is the exact source asset. Deterministic extraction rejects duplicate JSON keys, validates all addresses/amounts, and retains all **8,893** allocation pairs, including two zero balances. Numeric address sorting changes order only; no record is filtered or aggregated. A separate re-read of generated literals equals the complete ordered source extraction. All source allocation records contain only a balance field. The loader selects the allocation object and creates accounts from those addresses and converted balances; its other account fields use defaults.

`GenesisFundingData.lean` contains every address/credit pair in 70 chunks. `GenesisFundingInput.lean` supplies kernel checks of the complete entry count, address width, exact credit sum **72,009,990,499,480,000,000,000,000 wei**, and strict bound **< 2^96**. Both targeted compilations passed with exit 0 and emitted oleans. `allocation_count` uses no axioms; the other four printed exports use only `propext`. No `native_decide` or assumed arithmetic total is used.

The JSON receipt embeds the complete regeneration script, its hash, source hashes and extraction validation. Run that script from the repository root to regenerate the data without fetching external resources.

These are finite data and arithmetic facts, not a protocol genesis or supply theorem. The parser-to-data relation is reproducibility evidence, not kernel-proved; correspondence to the reference loader's initialized account world remains open. Canonical genesis/state-root adoption, initial-state assumptions, post-genesis issuance, external credit provenance, and history/queue/fee invariants are separate obligations. Thomas has not adopted the proposed reference/mainnet choice; shared pins and public theorem metadata are unchanged.

Frozen proof hashes: `GenesisFundingData.lean` = `62d6e9c792bc75db9c713deaacd42d497e2f3df15fc17194f63ab6f35f9fc1ad`; `GenesisFundingInput.lean` = `129d39db51cb813e986a8b4ad20610303edc0ee8f550c386b7cb52d9423823e4`. Exact commands and complete short compile logs are embedded in the JSON receipt. No broad build was run by this worker.
