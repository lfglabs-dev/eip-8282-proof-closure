# Pinned evaluator fuel boundary

Source: `Eip8282/Audit/Integrator/EvaluationFuelBoundary.lean`, SHA256 `289167ce2f08074de57c5a37490fa5fe1d901f52133ff40d85fa44630def3cde`.
Targeted `lake env lean -o ...` completed exit 0. Log: `/tmp/eip-evaluation-fuel-boundary-compile.log`. All seven printed theorem dependency lists contain only propext, Classical.choice, Quot.sound.

Certified artifact: diagnostic runtime `5f5f5ff000`, owner 100, protected account 200 with balance 7, initial gas 1,000,000, literal XiTransport empty-stack entry. Four accepted Z/Step transitions form XRuns at initial X fuel 5: three PUSH0 instructions followed by CREATE. The selected Lambda has fuel zero and returns OutOfFuel. The CREATE dispatcher catches it, producing the exact afterCreate with empty account map. The protected account changes from some account to none. The complete X 5 run ultimately returns OutOfFuel. This is an evaluator prefix artifact, not a canonical transaction or successful outer-message-call counterexample; audited runtime pins are untouched.

The proposed SSTORE continuation is refuted universally: if the owner is absent, State.sstore key value equals its input state. The exact erased world therefore stays empty under SSTORE. Source: pinned EvmYul/StateOps.lean lines 130–161, particularly lookupAccount.option self. A second CREATE that rebuilds a nonempty world remains an unproved proposal, not a certified counterexample. Its concrete address computation also encounters opaque extern keccak256, so kernel evaluation of hashing is unavailable.

Actual semantic boundaries: pinned EVM/Semantics.lean lines 295 and 353 catch every Lambda exception as empty world; Lambda fuel zero is line 602. Theta propagates OutOfFuel at line 810 and restores an empty returned world at line 818; this prefix alone does not defeat that final rollback. Lambda address-preimage absence can separately throw via the Option-to-Except lift (lines 145–146 and 609), so merely excluding caught OutOfFuel does not cover all creation exceptions.

Required producer obligation for protected histories: for every actually selected CREATE/CREATE2 child in the execution certificate, derive a completed Lambda `.ok` result (either success flag), rather than assume desired world preservation. This can follow from a constructive evaluation-fuel adequacy theorem plus total successful address-preimage encoding under actual nonce/init/salt bounds. Alternatively prove every caught exception branch cannot commit harmful effects, including later world reconstruction; that stronger rollback statement is presently open. Top-level `.ok` alone has not been proved sufficient. Neither evaluator fuel nor this diagnostic is an Ethereum protocol rule. Canonical histories still need their actual resource/encoding producer or an explicit residual obligation.
